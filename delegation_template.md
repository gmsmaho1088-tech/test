# 自動化の任せ方テンプレート

このテンプレは、AIに「どこまで自動で任せるか」を最初に明確化するためのものです。
そのまま貼って使えます。

## 1) まずは安全寄り（推奨スタート）

```text
目的:
- （例）既存APIの不具合を直し、テストを通す

自動で進めてよい範囲:
- 調査、原因切り分け
- 実装修正
- テスト実行
- 変更内容の要約

事前確認が必要な範囲:
- 依存ライブラリ追加
- DBスキーマ変更
- 外部API契約の変更
- 破壊的変更

禁止事項:
- 本番環境への変更
- 秘密情報の出力

成果物:
- 変更ファイル一覧
- 実行コマンドと結果
- 次のアクション提案（3つ以内）
```

## 2) 高速実行モード（レビュー前提）

```text
目的:
- （例）UI改善を一気に実装してPRまで作る

自動で進めてよい範囲:
- 設計判断を含む実装
- リファクタリング
- テストの追加/更新
- コミットとPR作成

事後報告でよい範囲:
- 軽微な依存更新（パッチバージョン）
- ドキュメント更新

停止して確認する条件:
- 主要仕様の解釈が2通り以上ある
- 既存テストが広範囲で失敗する
- セキュリティ/性能に影響する変更
```

## 3) 最小確認モード（初心者向け）

```text
1ステップごとに確認して進めて。
毎回、次の形式で出力して:
- やること
- 変更点
- 実行コマンド
- 想定リスク
- 続行してよいか
```

## 4) そのまま使える指示文（短縮版）

```text
「目的は◯◯。自動で調査→実装→テスト→要約まで進めて。
ただし、依存追加・DB変更・破壊的変更の前は必ず止まって確認して。」
```

## 5) 運用のコツ（短く）

- 最初は「安全寄り」で始める
- 2〜3回うまく回ったら「高速実行」に上げる
- 止まってほしい条件を明文化する
- 最後に「何を変えたか」を必ず箇条書きで受け取る
